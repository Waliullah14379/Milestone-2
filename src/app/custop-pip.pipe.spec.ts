import { CustopPipPipe } from './custop-pip.pipe';

describe('CustopPipPipe', () => {
  it('create an instance', () => {
    const pipe = new CustopPipPipe();
    expect(pipe).toBeTruthy();
  });
});
